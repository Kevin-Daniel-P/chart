# Minimal Redis config
architecture: standalone

auth:
  enabled: false  # Simplest for testing

image:
  tag: 7.2.4-debian-12-r9  # Valid tag

master:
  persistence:
    enabled: false
  resources:
    requests:
      memory: 128Mi
      cpu: 100m
    limits:
      memory: 256Mi
      cpu: 200m
